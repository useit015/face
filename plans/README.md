# Implementation Plans

Generated by the improve skill on 2026-09-01, against a working tree on top
of commit `2d317f3` (the tree contained uncommitted WIP — each plan's drift
check accounts for this). Execute in the order below unless dependencies say
otherwise. Each executor: read the plan fully before starting, honor its
STOP conditions, and update your row when done.

**Default applied**: the selection prompt went unanswered, so plans were
written for the top 5 findings by leverage (findings 1, 2, 4, 7, 8 from the
audit); the maintainer then rejected the stars fix (001) and CI (005) —
see below. Unplanned findings are listed at the bottom.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 002 | Set theme-color meta to match the active theme on first paint | P1 | S | — | DONE (verified 2026-09-01) |
| 003 | Make contribution-graph cells keyboard-accessible and screen-reader labeled | P2 | S-M | — | DONE (verified 2026-09-01) |
| 004 | Replace boilerplate README with real project documentation | P3 | S | — | DONE (verified 2026-09-01) |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- No dependencies between the remaining plans; 002–003 are independent code
  changes, 004 is docs-only.
- All plans share the same verification gates: `pnpm exec tsc --noEmit`,
  `pnpm exec eslint .`, `pnpm build`.

## Rejected by maintainer (2026-09-01)

- Plan 001 (fetch stars for every project with a repo): rejected by the
  maintainer — not wanted.
- Plan 005 (CI workflow): rejected by the maintainer — not wanted.

## Findings considered and rejected

- Dev-dependency major updates (`@types/node` 20→26, eslint 9→10,
  TypeScript 5.9→7.0): no vulnerabilities (`pnpm audit` clean), dev-only,
  TS 7 is a major-jump rewrite — revisit deliberately, not via a plan.
- Unauthenticated GitHub API rate limiting in `lib/stars.ts`: 2 requests
  with 1-hour ISR caching cannot realistically hit the 60/hr anonymous
  limit at this site's traffic.
- Duplicated `ContributionDay` type and `MONTHS` array across
  `contribution-graph.tsx` / `contribution-cells.tsx`: trivial; fold into
  the next edit of those files rather than spending a plan on it.
- Test suite (graph week/month-label bucketing is the only pure logic worth
  testing): low value for a static portfolio; deferred until the site grows
  logic-heavy code.
- External favicon service (`emojifavicons.com`): alternative direction
  option (self-hosted SVG) not selected in this run.

## Direction options (not planned; maintainer's call)

- **SEO/social**: `openGraph` metadata exists but has no image and no
  `metadataBase`; no JSON-LD `Person`, no sitemap/robots. Highest-value
  direction for a portfolio's discoverability. Requires producing an OG
  image asset.
- **Self-hosted favicon**: replaces the third-party emojifavicons.com
  dependency (and its `ref` parameter) with a committed SVG; loses the
  automatic light/dark emoji swap unless replicated.
